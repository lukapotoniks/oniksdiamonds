[build]
  publish = "."
    command = "node build-blog.js && node build-kategorije.js && node build-analytics.js && node build-schema.js && node build-sitemap.js"
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
